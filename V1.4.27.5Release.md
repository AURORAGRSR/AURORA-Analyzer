# AURORA-Analyzer V1.4.27.5 发布说明

***

## 1. 欢迎使用 V1.4.27.5

感谢您选择 AURORA-Analyzer！V1.4.27.5 是动效与材质系统的全面升级版本。我们将启动画面（Splash Screen）独有的星空缩放动效，统一移植到了应用内的每一个窗口；对 V5 玻璃材质管线进行了系统性优化；并为按钮和控件引入了果冻弹性动效，让每一次交互都充满生命感。

***

## 2. 统一的窗口动效语言

### 从宇宙中浮现

此前，各窗口的入场和退场动效参数各不相同——有的窗口放大入场、有的缩小入场，有的窗口退场时收缩、有的仅淡出。V1.4.27.5 将所有窗口统一为同一套动效：

- **入场**：窗口从 1.15 倍缩放优雅收敛至 1.0，伴随淡入，像星辰从远处浮现
- **退场**：窗口从 1.0 倍缩放放大至 1.15，伴随淡出，像星辰消散于远方

这套动效的核心视觉隐喻是"从宇宙中浮现，向宇宙中消散"，与应用的星空背景主题完美呼应。

### 更绵长的退场仪式

退场动画从 450ms 延长至 800ms，采用更柔和的缓动曲线。窗口关闭时不再急促地消失，而是像星光一样缓缓放大、淡去，为每一次告别留出从容的仪式感。

***

## 3. 更通透的液态玻璃材质

V1.4.27.5 对 V5 玻璃材质管线进行了系统性优化，让每一块毛玻璃都更通透、更精致：

### 模糊与饱和度校准

修复了模糊后端的硬编码问题和分辨率补偿缺失，将 AuroraFluentGlass 风格的 BlurRadius 从 12 校准至 8（修复后端后模糊量翻倍过强），同时将饱和度从 1.0 提升至 1.4（Mica vibrance +40%），让玻璃背后的色彩更鲜艳通透。

### 14 层光学层逐层打磨

玻璃材质由 14 层光学效果叠加而成。本次对每一层都进行了参数精调：基底层从纯冷蓝改为环境色混合，镜面高光新增次级光斑和呼吸漂移，菲涅尔层从单一径向渐变重写为四方向线性渐变，斜面层从 2 段渐变升级为 4 段渐变，边缘高光降低了过强的峰值避免白边。新增了焦散光斑层和虹彩干涉层，让玻璃表面呈现出更丰富的光学质感。

### 渲染性能优化

将模糊渲染从同步改为异步两阶段流水线，UI 线程 CPU 开销从 4ms/帧降至 1.5ms/帧。移除了重复的节流逻辑，消除了 ~66ms 的延迟，让极光模糊更紧密地跟随窗口移动。

***

## 4. 果冻弹性交互动效

V1.4.27.5 为按钮和控件引入了果冻弹性（Jelly Elastic）动效，让交互充满生命感：

### 按钮 hover 离开果冻回弹

当鼠标离开按钮时，缩放不再线性回到 1.0，而是经历三阶段果冻回弹：先惯性下冲到 0.96（模拟释放后的弹性形变），再回弹到 1.02（玻璃弹性恢复），最后优雅收敛到 1.0。整个过程约 271ms，幅度控制在 5% 以内，微妙而自然。

### iOS 风格 Q弹点击反馈

按钮点击释放时，缩放从 0.98 轻微过冲到 1.02 再收敛到 1.0，呈现 iOS 风格的 Q弹手感。幅度仅 2%，几乎不被察觉，却让点击有了"按压回弹"的触感。

### 磁吸回弹改进

旧版磁吸归位使用 3.5% 过冲的阻尼曲线，offset 会越过中心到反侧。新版改为纯 EaseOutCubic 曲线，无过冲、无反向偏移，磁吸归位像"轻物自然落定"，更贴合毛玻璃的柔和质感。

### 控件交错入场过冲收敛

控件交错入场时，每个控件先过冲到 1.07 倍，再以 525ms QuarticEase 优雅收敛到 1.0，呈现液态玻璃的弹性质感。窗口入场完成后也会追加 1.0→1.03→1.0 的微回弹，退场时先微缩到 0.97 蓄力再放大离开。

***

## 5. 对话框窗口的动效补全

### 提权选择窗口

此前该窗口的缩放变换被错误地放在了内层容器上，导致入场动画无法正确收回缩放，控件看起来始终被放大了 10%。V1.4.27.5 修复了这一问题，并将动效对齐为 Splash 风格。

### 会话恢复窗口

此前该窗口没有任何窗口级动效，出现和消失都是瞬间的。现在它也拥有了从星空浮现、向星空消散的完整动效。

### 性能升级询问窗口

此前该窗口采用缩小式入场（0.92→1.0）和简单淡出退场。现在统一为放大收敛入场和放大冲出退场，与其他窗口保持一致。

***

## 6. 问题修复

### 提权窗口控件大小异常

修复了提权选择窗口控件被异常放大的问题。原因是缩放变换被放在了错误的容器层级，导致入场动画找不到目标，缩放永不被收回。

### 星空定时器泄漏

修复了部分对话框窗口关闭后星空动画定时器仍在后台运行的问题，杜绝了内存泄漏。

***

## 7. 升级须知

V1.4.27.5 与 V1.4.27.1 完全兼容，所有命令行参数、配置文件和脚本接口均无需修改。直接替换程序文件即可完成升级。

***

## 8. 系统要求

| 项目             | 最低要求             |
| -------------- | ---------------- |
| 操作系统           | Windows 10 或更高版本 |
| .NET Framework | 4.8 或更高版本        |
| 内存             | 建议 4GB 以上        |
| 屏幕分辨率          | 建议 1366 × 768 以上 |

***

> **© AURORA VelociRaptor-GR Dev PRJ. All rights reserved.**
>
> *本工具仅供个人学习使用。请遵守当地法律法规。*
