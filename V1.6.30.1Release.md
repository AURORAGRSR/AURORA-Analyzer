# AURORA-Analyzer V1.6.30.1 发布说明

***

## 1. 欢迎使用 V1.6.30.1

感谢您选择 AURORA-Analyzer！V1.6.30.1 是"执行流程健壮性与回滚闭环完整性"专项加固版本。我们对解决方案视图的执行窗口进行了全面重构，让每一步修复进度都清晰可见；完成了完整的代码审计，共修复 21 个缺陷（6 个严重 + 12 个中等 + 3 个低危）；同时把所有玻璃弹窗的动画实现统一到一处管理，让动画体验更一致。MSBuild 编译通过零错误零警告，开箱即用。

本次更新围绕三个方向展开：**更强大的执行流程**、**更可靠的回滚闭环**、**更统一的动画体验**。

***

## 2. 更强大的修复执行窗口

### 步骤时间线 + 日志双栏布局

V1.6.30.1 把执行界面升级为左右双栏：左侧实时展示每一步修复的进度阶段（Starting / Executing / RolledBack / Completed / Skipped），右侧同步回显命令日志。您可以一边看进度推进，一边查看具体命令输出，再也不用来回切换视线。

### 失败时可选"继续"或"中止"

之前的版本一旦遇到失败就只能"中止"。V1.6.30.1 新增"继续"选项——选择继续时，会先执行失败命令的回滚命令，再跳过该步继续后续流程，避免脏状态引发的连锁失败，把主动权交还给用户。

### 智能预估剩余时间

执行窗口会基于已执行命令的平均耗时，实时估算"还需多少时间"。无需盯着进度条发呆，看一眼就能心里有数。

### 取消修复时彻底终止子进程

此前取消修复可能留下"孤儿进程"在后台继续跑。V1.6.30.1 改用进程树级别的终止机制，取消时一并杀掉所有衍生子进程，不留尾巴。

### 预校验过滤

执行前先对每条命令做前置条件检查，自动跳过当前环境不适用的命令。您看到的每一步都是真正会被执行的，不再有"跑了等于没跑"的空操作。

***

## 3. 更可靠的撤销回滚

### 撤销失败可重试

此前撤销失败后按钮会永久禁用，用户只能干瞪眼。V1.6.30.1 保留了可重试状态——撤销失败后您可以直接再点一次重试，不必重启程序或放弃修复。

### 孤儿快照自动清理

进程意外退出后，磁盘上可能残留一些"无主"的备份快照。V1.6.30.1 启动时会自动清理 7 天以上的过期快照，避免磁盘被无意义的旧快照占满。

### 撤销查看器显示磁盘快照

即使会话信息已经丢失，磁盘上残留的备份快照也会在撤销查看器中以"孤儿快照"形式显示出来。您能看到它、能管理它，不会再有"看不见但占空间"的情况。

### 撤销成功后界面立即刷新

修复了撤销成功后界面状态不更新的问题——现在撤销一完成，相关界面立刻同步到最新状态，无需手动刷新或重启窗口。

***

## 4. 更稳定的视图切换

### 6 个视图退场定时器泄漏修复

修复了快速切换视图时退场定时器资源累积泄漏的问题。6 个视图（专业模式、智能模式、历史模式、解决方案详情、权限对话框、会话恢复）全部完成修复，长时间频繁切换也不再卡顿。

### 控制台日志订阅正确管理

修复了从专业模式或智能模式返回后，控制台可能不回显日志的潜在风险。订阅关系现在被正确地创建与释放，模式来回切换后控制台始终能正常输出。

### 入场动画定时器在视图卸载时清理

视图卸载时，对应的入场动画定时器会被一并清理，避免卸载后定时器仍触发导致的状态异常。这就是您感觉切换"更跟手"的原因。

***

## 5. 更严格的终态保护

### 修复会话不会被错误覆盖

撤销成功的会话，不会再被延迟到达的完成回调错误地覆盖为其他状态。终态就是终态，一旦确认就锁死，不会被异步时序搅乱。

### 空方案正确显示"部分成功"

当所有命令都被预校验过滤、实际一条都没执行时，状态会显示为"部分成功"而不是"成功"。语义更准确，避免给您造成"全部修好了"的错觉。

### Continue 模式执行回滚命令

选择"继续"执行时，失败命令对应的回滚命令会被正确执行。不会再出现"跳过了但没回滚"的脏状态残留，保证后续命令在一个干净的环境里继续跑。

***

## 6. 更统一的玻璃弹窗动画

### 10 处弹窗动画统一管理

此前有 10 个对话框各自实现相同的入场/退场动画代码，散落在不同文件里。V1.6.30.1 把这些重复实现统一封装到 `GlassDialogAnimation` 工具类，一处定义、处处复用。

### 动画参数集中调整

以后任何动画时长的调整都只需修改一处，不会再出现"改了 9 处漏了 1 处"的同步问题。维护更省心，体验更一致。

### Window 级退场动画统一

性能诊断对话框和性能升级对话框的退场动画也被纳入统一管理。无论弹窗挂在哪个层级，动画表现都完全一致。

***

## 7. 历史分析与报告导出体验升级

### 分析按钮智能切换

此前历史界面底部有"趋势"和"时间线"两个按钮，您得自己判断该用哪个。V1.6.30.1 把它们合并为一个"分析"按钮，它会根据您选中的归档数量自动切换：没选时显示"分析"（灰色禁用），选中 1 条时变成"时间线"（时钟图标），选中 2 条及以上时变成"趋势"（折线图标）。按钮上还会显示对应的小图标，一眼就知道当前是什么分析模式。

### 多选后按钮即时刷新

修复了批量选中归档后按钮文字和图标不更新的问题——此前由于批量同步期间通知被挂起，选完之后按钮还停留在旧状态。现在批量选择完成后会立即补刷一次，按钮文字、图标和批量删除按钮都会同步到最新状态。

### 趋势图只看选中的归档

修复了跨归档趋势图数据源错误的问题——此前选中 2 条归档查看趋势时，图表展示的是全部历史归档的趋势，而不是您选中的那 2 条。现在趋势图严格只反映您选中的归档，看什么就是什么。

### 报告格式选择不再"点全亮"

修复了报告格式对话框里三个格式选项无法正确互斥的问题——此前因为三个单选按钮分属不同的布局容器，WPF 默认的互斥范围失效，点哪个都全亮。现在通过统一的 GroupName 实现跨容器互斥，选一个就是一个，干净利落。

### 报告导出语言与主界面一致

修复了主界面是英文但报告格式对话框却是中文（或反过来）的语言不同步问题——此前对话框依赖全局语言服务，和主界面的语言设置脱节。现在语言会从主界面一路传递到对话框再到报告生成器，无论中英文都保持一致。

### 格式卡片鼠标悬停反馈

给报告格式对话框的三个选项卡片加上了鼠标悬停的渐变反馈——此前卡片没有任何 hover 效果，点击选择时也是生硬的颜色覆盖。现在鼠标移上去会平滑渐亮（200ms 过渡），移开渐灭，选中状态也有平滑的颜色过渡，和主历史界面的列表项反馈风格完全一致。

***

## 8. 更全面的代码审计

### 6 个严重缺陷（P0）全部修复

涵盖 Unloaded 死锁、撤销不可重试、CTS 泄漏、退出句柄未释放、退场无兜底、入场定时器未清空等问题。这些是会导致界面卡死、资源耗尽或不可恢复状态的核心缺陷，现已全部修复。

### 12 个中等缺陷（P1）全部修复

涵盖终态保护、快照清理、孤儿快照加载、事件订阅管理、动画统一封装等。这些缺陷平时不显眼，但长期累积会侵蚀稳定性，现已一并修复。

### 3 个审计新发现低危缺陷修复

包括空方案语义矛盾、终态分支数据不一致、撤销后 UI 不刷新。虽属低危，但为追求完整闭环也一并处理。

### MSBuild 编译通过零错误零警告

所有改动通过 MSBuild 编译验证，零错误零警告，干净利落。

***

## 9. 兼容性说明

V1.6.30.1 与历史版本完全兼容：

- 所有命令行参数、环境变量、PowerShell 脚本接口完全兼容，无需修改任何调用方式。
- 中英双语界面完全支持，两种语言用户均可无缝使用。
- V5 材质管线与玻璃材质捕获精度不变，既有视觉效果与采集结果保持一致。

直接替换程序文件即可完成升级。

***

## 10. 系统要求

| 项目             | 最低要求             |
| -------------- | ---------------- |
| 操作系统           | Windows 10 或更高版本 |
| .NET Framework | 4.8 或更高版本        |
| 内存             | 建议 4GB 以上        |
| 屏幕分辨率          | 建议 1366 × 768 以上 |

***

> **© AURORA VelociRaptor-GR Dev PRJ. All rights reserved.**
>
> *本工具仅供个人学习使用。请遵守当地法律法规。*
